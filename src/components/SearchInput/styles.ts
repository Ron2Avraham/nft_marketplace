import { StyleSheet } from "react-native";

export default StyleSheet.create({
    TextInputcontainer: {
        backgroundColor: "white",
        borderRadius: 10,
        fontSize: 20,
        color: "black",
        margin: 20
    },
});